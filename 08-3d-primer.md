# 3D AI 全景导读：从表征到世界模型

> 写给想建立完整坐标系的人。尽量白话，但每条结论都对应真实技术脉络。  
> 基准日期：2026-08-05  
> 相关工作清单：[`07-related-work-3d.md`](./07-related-work-3d.md)  
> 与 World Model 交叉：[`01-related-work.md`](./01-related-work.md)

---

## 开场：为什么 2023 之后“3D”突然变成显学

过去十年，深度学习最猛的是**图像和语言**——平面世界。3D 一直被认为又难又贵：数据少、标注贵、表示不统一、算得慢。

2020 年 **NeRF** 证明：只要多视角照片 + 可微渲染，网络就能“记住”一个场景并从新角度画出来。  
2023 年 **3D Gaussian Splatting** 证明：这件事还能**实时**。  
2024 年 **DUSt3R** 证明：甚至可以不标定相机，直接从几张乱序照片吐出三维点。  
2024–2026 年 **TRELLIS / Hunyuan3D** 证明：物体级 3D 生成可以像“文生图”一样进流水线。  
同期 **Genie / Marble** 把争论推到终局：世界模型到底该是**一段会动的视频**，还是**一个可导出的三维世界**？

如果你只记住一句话：

> **3D AI 不是一个算法，而是一条流水线：表征 → 获取 → 渲染 → 生成 → 理解 → 时间 → 智能体使用。**

下文按这条流水线展开。

---

## 第一章　表征：3D 的“货币”

### 1.1 先建立一个直觉

所谓 3D 表征，就是：**用某种数据结构，把“空间里有什么”编码下来**，好让计算机能渲染、编辑、碰撞、规划。

没有万能表征。好看、好算、好编辑、好仿真，经常互相打架。选表征就像选货币——后面所有交易都得用它。

### 1.2 七种主流“货币”

| 表征 | 通俗比喻 | 你得到什么 | 你失去什么 |
|---|---|---|---|
| **点云** | 空中撒了一把沙子 | 传感器直接给、稀疏几何清楚 | 没有“皮”，不好看、不好碰撞 |
| **体素 / 占用栅格** | 乐高积木格 | “这里能不能走”很清楚 | 格子细了内存爆炸 |
| **网格 Mesh** | 三角形缝的皮肤 | 游戏/CAD/打印全都认 | 拓扑难搞，细节很贵 |
| **SDF** | 问每个点“离表面多远” | 表面光滑，布尔运算方便 | 薄片、开口、衣服褶皱很痛苦 |
| **NeRF** | 问“朝这个方向看是什么颜色” | 新视角很美 | 慢；也不直接给你一个可导出模型 |
| **3D Gaussian** | 百万个彩色小椭圆（高斯斑） | **又快又好看，还能反传训练** | 几何发噪、文件大、不等于干净网格 |
| **混合 / Latent** | 几套系统拼起来，或压成密码 | 生成友好、可解码多种格式 | 管线复杂 |

### 1.3 2026 年的现实格局

- **要实时看、要交互浏览** → 默认 **3DGS**
- **要进游戏引擎 / 做碰撞 / 给艺术家改** → 最终仍要 **Mesh**（常常从 GS/SDF 提）
- **要做驾驶规划** → **Occupancy（占用）** 比漂亮外观更重要
- **要做生成模型** → **VecSet** 或 **稀疏结构 latent（TRELLIS 系）**

### 1.4 常见误区

1. 「NeRF 就是 3D 模型」→ 它更像**可渲染的外观场**，不是干净网格。  
2. 「Gaussian 已经取代一切」→ 它赢在**新视角合成的性价比**，不是测量学和工业 CAD。  
3. 「隐式一定更准」→ 连续 ≠ 准确；多视角不一致一样毁掉表面。

**一句话：** 表征决定你后面能干什么；先问任务，再选货币。

---

## 第二章　获取：怎么从真实世界“拿到”3D

### 2.1 三条路

1. **多视角几何（SfM / MVS）**  
   多张照片 → 先估相机 → 再三角化出点/深度。代表：**COLMAP**。慢但准，测绘和影视仍常用。

2. **主动深度传感**  
   RGB-D 相机、iPhone LiDAR、车载激光雷达。优点是有**真实尺度（米）**；缺点是噪声、范围、户外阳光干扰。

3. **学习式一步到位（feed-forward）**  
   网络直接吐几何。2024 年后的主航道。

### 2.2 DUSt3R 带来的范式转移

传统思路：先有相机，再谈三维。  
**DUSt3R（CVPR 2024）** 反过来：先预测每个像素对应的 3D 点（**pointmap**），再反推相机。

这为什么重要？

- 不要求你先标定、先配对；
- 单张、两张、多张可以统一处理；
- 把“三维视觉”变成了类似“深度估计”的稠密预测问题，Transformer 就能上。

### 2.3 一条必须认识的家族谱系

```text
DUSt3R (2024)
  → MASt3R（更会匹配，有度量）
  → Spann3R / CUT3R / Fast3R（在线、多图、快）
  → VGGT（2025 最佳论文：一次前向出相机+深度+点图+轨迹）
  → π³ / DA3 / VGGT-Ω / D4RT（2026：更快、更稳、能量缩放、会问 4D）
```

你可以把它想成：  
**“三维界的基础模型时刻”**——不是再为每个场景苦苦优化，而是一个大网络见过足够多场景后，直接“看图说话（成几何）”。

### 2.4 但它没有杀死 COLMAP

2026 年的严肃比较（如 GLUEMAP）表明：

- 在**干净、高重叠、标定良好**的数据上，经典 SfM 往往**仍然更准**；
- 在**野生、弱纹理、乱序网络照片**上，feed-forward 往往**更稳、更能出活**；
- 工程上越来越多是 **VGGT/DA3 初始化 + 经典 BA 精修**。

### 2.5 尺度这件大事

纯图像有一个物理硬伤：**你分不清玩具屋和真房子**（尺度歧义）。  
“度量深度 / 度量重建”之所以难，就是要在没有尺子的情况下猜出米制单位——通常要靠传感器、物体尺寸先验，或大规模数据里学到的统计规律。

**一句话：** 获取把像素变成可操作的三维状态；它是一切下游的上游。

---

## 第三章　渲染：为什么 3DGS 赢了这一轮

### 3.1 渲染是什么

渲染 = **从三维状态生成像素**。三条经典路：

| 路线 | 做法 | 好比 |
|---|---|---|
| 光栅化 | 投影三角形，填像素 | 游戏引擎，快 |
| 光线行进 / 体渲染 | 沿视线采样累加颜色 | NeRF，美但慢 |
| Splatting（泼溅） | 把椭圆斑泼到屏幕上再混合 | 3DGS 的关键 |

### 3.2 NeRF 解决了什么

NeRF 的贡献不是“发明了一种好看的画法”，而是证明：

> **可微渲染 + 多视角照片损失 = 可以从图像反推场景。**

这打开了“用图像监督三维”的大门。

### 3.3 3DGS 为什么赢

3DGS（Kerbl et al., 2023）做对了三件事：

1. 用**显式高斯椭圆**代替慢 MLP；
2. 用**可微的 tile 光栅**代替昂贵体渲染；
3. 训练中穿插**增密/剪枝**，让细节自己长出来。

结果是：**NeRF 级画质 + 实时 1080p**。  
于是研究问题从“能不能像 NeRF 一样好看”变成了：

- 抗锯齿（Mip-Splatting）
- 压缩与 LOD（Scaffold / Octree / CityGaussian）
- 表面提取（2DGS / SuGaR）
- 城市尺度（Hierarchical 3DGS）
- 动态（4DGS）

### 3.4 “看起来对”≠“量起来准”

这是整本导读里最重要的句子之一。

高斯可以“长”出非常骗过眼睛的高光和纹理，但表面可能是糊的、漂的、穿透的。  
所以工业管线常常是：

```text
照片 →（可选）feed-forward 几何初始化 → 3DGS 外观
      → 2DGS/SuGaR/SDF 提网格 → 给引擎/仿真
```

**一句话：** 渲染既是显示器，也是训练信号；3DGS 赢的是性价比，不是几何真理。

---

## 第四章　生成：从“做梦”到“流水线”

### 4.1 物体生成的三代故事

**第一代：SDS（2022–2023）**  
DreamFusion 用 2D 文生图模型当老师，反反复复优化一个 3D，直到渲染图让老师满意。  
能出东西，但慢、容易“多脸”（Janus）、颜色过饱和。今天只作历史。

**第二代：多视图 + 大重建模型（2023–2024）**  
先生成一致的多视角图，再喂给 LRM / InstantMesh / TripoSR 一秒出网格。  
快了，但几何常常“看起来像、结构歪”。

**第三代：原生 3D 扩散 / 流匹配（2024–2026）**  
直接在 3D latent 上生成。两条主流 latent：

- **VecSet**（一袋无序 token）→ Hunyuan3D / TripoSG / CLAY
- **稀疏结构 latent（SLAT / O-Voxel）** → TRELLIS / TRELLIS.2

再加第二阶段画 **PBR 材质**（反照率、金属度、粗糙度）。

> 2026 年物体级的“标准菜谱”已经定了：  
> **图像条件 → 3D latent 流匹配 → 网格 → PBR 贴图。**

### 4.2 文本生 3D 的真相

很多“文生 3D”其实是：

```text
文本 → 文生图 → 图生 3D
```

因为图像条件比纯文本稳得多。别被产品页误导。

### 4.3 场景级生成：另一种难度

单个椅子 ≠ 一整座可走的城市。

场景级成功的系统几乎都在做拼装，而不是一个神仙网络端到端硬刚：

1. **脚手架**：LLM 写布局 / 程序化规则 / 道路地图 / 全景分层  
2. **抬升**：每个物体用 image-to-3D，或视频先验蒸馏成 3DGS  
3. **导出**：navmesh、碰撞体、引擎格式

代表：WonderWorld、HunyuanWorld、WorldGrow、WorldGen、**Marble**。

### 4.4 真正的瓶颈叫“资产生成化”

生成一个漂亮形状只是开始。游戏/仿真还要：

- 干净拓扑（边循环、四边面）
- UV 展开
- 贴图不烘光照（可重打光）
- 部件拆分、骨骼绑定
- LOD、碰撞体

2025–2026 很多认真工作都在攻这些“土”问题。  
评测也很诚实：人类投票往往奖励“有贴图的高斯”，而不是“拓扑正确的网格”——和产业需求正好反着。

**一句话：** 生成用数据先验补全未观测的维度；物体草稿已可用，生产级资产和大型可交互场景仍未解决。

---

## 第五章　理解：机器如何读懂房间与街道

### 5.1 理解 ≠ 重建

重建回答：“长什么样？”  
理解回答：“是什么？在哪？能不能走？离我多远？能不能抓？”

### 5.2 驾驶里的进化路径

```text
2D 检测
  → BEV（鸟瞰特征）+ 3D 框
  → Occupancy（每个空间格子是否被占）
  → 占用世界模型（预测未来格子怎么变）
  → 端到端规划（UniAD / VAD / NAVSIM 时代）
```

占用为什么重要？因为锥桶、施工区、异形障碍物不一定是训练集里的“车/人”类别，但规划必须知道**哪里不能撞**。

### 5.3 开放词汇：让三维听得懂人话

OpenScene / Uni3D 一类工作把 CLIP 的语言空间对齐到点云或多视角特征上。  
于是你可以问：“把所有可坐的东西找出来”，即使训练集没标“凳子”。

代价是：它很大程度上仍是**“2D 基础模型抬到 3D”**，不是原生三维语言理解。

### 5.4 空间 VLM：会聊天 vs 会量尺寸

2024 年后出现一批专门练空间问答的模型（SpatialVLM、SpatialRGPT、SpatialBot…）。  
2025 年后更关键的趋势是：把 **VGGT 这类几何变压器**塞进 VLM，当作“眼睛里的三维模块”。

务必分清：

| 能力 | 例子 | 难度 |
|---|---|---|
| 定性空间 | “杯子在盘子左边” | 相对容易 |
| 度量空间 | “大约 35 厘米” | 很难 |
| 可操作空间 | “这样抓不会碰倒” | 极难 |

**一句话：** 理解给三维挂上可读标签与问题接口；没有它，重建只是漂亮几何。

---

## 第六章　时间：当 3D 开始会动（4D）

### 6.1 4D = 3D + 时间

既要新视角，也要新时刻。两条技术路：

1. **变形场**：一个“标准姿势”的 3D + 随时间扭动  
2. **原生 4D 原语**：高斯/体素直接活在时空里

### 6.2 重建式 4D vs 生成式 4D

- **重建式**：给你多相机或单目视频，恢复动态场景（4DGS、Shape of Motion、MoSca、D4RT）  
- **生成式**：给文本/图像/视频，变出一个会动的资产（L4GM、SV4D…）

别把“视频扩散会动”直接叫“4D 重建”——前者出像素，后者要可渲染的时空结构。

### 6.3 难度天梯

| 设定 | 难度 | 2026 状态 |
|---|---|---|
| 工作室多相机、短片段 | 中 | 实时回放基本可用 |
| 手机单目、随意拍摄 | 高 | 能出 demo，易在遮挡/非刚性上翻车 |
| 多物体、长时程、可仿真 | 极高 | 基本未解决 |
| 驾驶 4D 占用预测 | 另成一派 | 更关心安全与可规划，不关心毛发级外观 |

**一句话：** 4D 把数字孪生推进到“会演化的世界”；看起来在动很容易，长期一致且可仿真很难。

---

## 第七章　智能体如何使用 3D

### 7.1 智能体不需要最美的 3D

它需要：

1. **我在哪**（定位）  
2. **周围是什么**（建图/感知）  
3. **下一步会怎样**（预测）  
4. **我该怎么做**（规划/策略）

### 7.2 SLAM：边走边建

经典 ORB-SLAM / VINS 用稀疏特征。  
2024 起出现一批 **GS-SLAM**：地图本身就是可渲染的高斯，走完能留下来一段漂亮的三维记忆。  
2025–26 又和 VGGT/MASt3R 结合，追求**免标定、更稠密、能上机器人**。

难点仍是：动态物体、大尺度漂移、算力与边缘端。

### 7.3 两套世界观：Marble vs Genie

这是 2025–2026 最重要的分歧，写论文前必须先站队或杂交。

| | **显式世界（Marble 系）** | **隐式世界（Genie 系）** |
|---|---|---|
| 存的是什么 | 高斯 / 网格 / 占用 | 下一帧像素或 token |
| 像什么 | 游戏关卡编辑器 | 无限续写的互动梦境 |
| 强项 | 可导出、可碰撞、可验证、长期几何 | 交互强、动态丰富、好用视频数据 |
| 弱项 | 开放交互与复杂动态难 | 会漂、难进物理引擎、几何不可审计 |
| 代表 | Marble, WorldGrow, Lyra, OccWorld | Genie 3, Matrix-Game, Cosmos |

World Labs 还提过一个有用的三分法：

- **Renderer**：负责“看起来像”  
- **Simulator**：负责“物理上说得通、状态可算”  
- **Planner**：负责“下一步动作”

很多争论其实是在抢这三个词。

### 7.4 和机器人的关系

机器人真正赚到钱的用法通常是：

- 用世界模型**造合成轨迹**再训策略（DreamGen 一类）  
- 用占用/点云做**规划与避障**  
- 用高斯/网格做**可编辑的场景记忆**  
- 在隐空间里做**短时程 MPC**（V-JEPA-2-AC 一类）

再次强调仓库里 world model 调研的共识：

> **视觉真实度是控制效用的弱代理。**

**一句话：** 智能体层把上游三维能力收束为感知—预测—行动闭环；漂亮、可控、可规划是三件不同的事。

---

## 第八章　2026 年前线地图

### 8.1 相对已经“能用”的

- 静态场景高保真新视角（尤其 3DGS）  
- 物体级 Image→3D 草稿（秒级）  
- 无标定图像集合的粗糙几何（DUSt3R 族）  
- 有充足标注的封闭集 3D 检测 / BEV  
- 研究原型级的动态新视角（多视角设定）

### 8.2 明显还没解决的

- 城市级、长期一致、可更新的可导航数字孪生  
- 单目视频 → 可仿真 4D（几何+物理+可编辑）  
- 场景级生成的布局可控、可扩展、可物理交互  
- 开放世界占用/检测的可靠性（安全关键）  
- 空间 VLM 的真度量能力  
- 从“看得像”到“推得对”：接触、流体、可变形、工具  
- World Model 评测：视觉指标 ≠ 任务成功  
- 数据与版权；端侧压缩

### 8.3 两条主冲突（写综述/开题很好用）

1. **显式持久世界 vs 隐式帧级想象**  
2. **互联网规模 2D 先验 vs 真实世界度量与物理**

几乎所有有趣的新工作，都是在这两条冲突上选边或杂交。

---

## 第九章　九大方向速查表

| # | 方向 | 核心问题 | 2026 关键词 | 入门地标 |
|---|---|---|---|---|
| 1 | 表征 | 世界用什么存？ | 3DGS / Mesh / Occ / Latent | NeRF, 3DGS, TRELLIS |
| 2 | 获取/重建 | 怎么从观测得到 3D？ | pointmap, VGGT, DA3 | COLMAP, DUSt3R, VGGT |
| 3 | 渲染 | 怎么看见它？ | splat, LOD, 表面化 | Instant-NGP, 3DGS, 2DGS |
| 4 | 生成 | 怎么无中生有？ | flow DiT, PBR, 场景脚手架 | DreamFusion→TRELLIS, Marble |
| 5 | 理解 | 怎么读懂它？ | Occ, 开放词汇, 空间 VLM | BEVFormer, OpenScene, SpatialVLM |
| 6 | 4D | 它怎么随时间变？ | 动态 GS, 视频→4D | 4DGS, Shape of Motion, L4GM |
| 7 | SLAM | 边走边建怎么做？ | GS-SLAM, VGGT-SLAM | SplaTAM, MASt3R-SLAM |
| 8 | 驾驶/机器人 | 怎么服务于行动？ | NAVSIM, 占用 WM, 位姿/抓取 | UniAD, OccWorld, FoundationPose |
| 9 | 世界模型 | 怎么预测+交互？ | Marble vs Genie, WAM | Genie 3, Marble, Cosmos |

---

## 第十章　支柱之间的关系（背下来就有坐标系）

| 从 → 到 | 关系 |
|---|---|
| 表征 → 渲染 | 表征决定渲染器类型、速度、真实感 |
| 获取 → 表征 | 传感器/算法决定你拿到点、体、场还是高斯 |
| 渲染 → 重建/生成 | 可微渲染提供训练梯度 |
| 生成 → 智能体 | 提供合成数据与可交互环境 |
| 理解 → 规划 | 语义与占用把几何变成可决策状态 |
| 4D → World Model | 时间演化能力是“预测未来”的核心 |
| 显式 WM ↔ 隐式 WM | 一个可导出可仿真，一个可规模化想象；正在互相借用 |

一张更短的总图：

```text
获取/生成 ──产生──▶ 表征 ──供──▶ 渲染/理解/4D
                         │
                         ▼
              SLAM / 规划 / World Model
                         │
                         ▼
                   机器人 · 驾驶 · 内容管线
```

---

## 第十一章　新手 30 天阅读路线

### Week 1｜表征与渲染
1. NeRF 原论文（懂可微体渲染）  
2. 3DGS 原论文（懂为什么能实时）  
3. 跑通一个 gsplat / nerfstudio 小场景  

### Week 2｜获取
1. COLMAP 流程直觉（不必会调所有参）  
2. DUSt3R → VGGT  
3. 一篇 feed-forward 综述（2507.08448）  

### Week 3｜生成
1. DreamFusion（只为懂 SDS）  
2. TripoSR / InstantMesh（懂第二代）  
3. TRELLIS 或 Hunyuan3D 2.1（懂第三代）  

### Week 4｜理解与智能体
1. BEVFormer 或 OccFormer 任选一篇  
2. OpenScene 或 SpatialVLM  
3. Genie 3 博客 + Marble 博客 + 本仓库 `01`/`03` 各读摘要  

### 读任何论文的五格法

```text
输入是什么？
表示是什么？
监督信号是什么？
输出给谁用？
评测是否测到了真正重要的东西？
```

比死记模型名有用一百倍。

---

## 第十二章　如何选研究方向（务实版）

先看三件事：**数据是否可得、评测是否诚实、下游是否真痛。**

### 2026 仍肥沃的缝（学术向）

1. feed-forward 几何 × 动态 / 驾驶  
2. 生成资产 → 可仿真（碰撞、质量、关节）  
3. 开放词汇占用 × 具身导航  
4. 显式世界 × 可学习规划器  
5. 压缩 / 流式 4D  
6. “视觉质量 ≠ 效用”的评测与训练目标  
7. 与 world model 记忆/反事实动作相关的机制题（见 `02-idea-candidates.md`）

### 新手避坑

- 只刷 PSNR/FID、没有下游任务  
- 又一个静态 3DGS 小改（除非卡在存储/鲁棒/物理）  
- 复现门槛极高且无开源当主线  
- 把 LIBERO / 单眼 FVD 当唯一 headline  

### 和 World Model 选题怎么衔接

如果你主线是 world model，3D 最有杠杆的接法通常是：

- 用 **显式几何当记忆 / verifier**（不是再训一个更大视频模型）  
- 做 **动作干预下的几何一致性评测**  
- 做 **相机位姿与化身位姿解耦**  
- 做 **画外区域仍随时间演化**  

这些在 `02-idea-candidates.md` 里有更具体的展开。

---

## 第十三章　资源入口

### 综述 / 地图
- 3D 表征：https://arxiv.org/abs/2410.06475  
- 学习式 3D 表征趋势：https://arxiv.org/abs/2606.04871  
- 3DGS 综述：https://arxiv.org/abs/2401.03890  
- Kerbl 展望：https://arxiv.org/abs/2510.26694  
- Feed-forward 重建：https://arxiv.org/pdf/2507.08448  
- 4D 生成：https://arxiv.org/abs/2503.14501  
- 生产向 3D 资产：https://arxiv.org/abs/2604.23629  
- 3D/4D World Modeling：https://worldbench.github.io/survey  
- World Labs 分类：https://www.worldlabs.ai/blog/taxonomy-of-world-models  

### 代码 / 产品
- DUSt3R：https://github.com/naver/dust3r  
- VGGT：https://github.com/facebookresearch/vggt  
- TRELLIS：https://github.com/microsoft/TRELLIS  
- Hunyuan3D 2.1：https://github.com/Tencent-Hunyuan/Hunyuan3D-2.1  
- Pointcept / PTv3：https://github.com/Pointcept/Pointcept  
- Awesome Gaussians：https://github.com/longxiang-ai/awesome-gaussians  
- Genie 3：https://deepmind.google/blog/genie-3-a-new-frontier-for-world-models/  
- Marble：https://www.worldlabs.ai/  

### 本仓库文档导航
| 文件 | 内容 |
|---|---|
| `07-related-work-3d.md` | 3D 全方向相关工作清单（本稿配套） |
| `08-3d-primer.md` | 你正在读的通俗导读 |
| `04-survey-3d-*.md` / `05-*.md` / `06-*.md` | 分方向详细调研 |
| `01` / `02` / `03` | World Model 相关工作、idea、具身驾驶 |

---

## 终章　用三句话收束

1. **3D AI 的本质是选表征、拿观测、可微地看见它、生成或理解它，再交给会行动的系统。**  
2. **2023–2026 的主旋律是：GS 让外观变实时，feed-forward 让几何变基础模型，native 3D 生成让物体进流水线，世界模型把“看”推进到“交互与决策”。**  
3. **下一阶段的胜负手不在更漂亮，而在更可导出、更可度量、更可规划、更可验证——也就是从“像真的”走向“当真的用”。**

如果你希望下一步把某一章扩成开题综述（例如只写“feed-forward 几何”或只写“显式世界生成”），直接指定方向即可。
