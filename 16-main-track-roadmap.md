# 主会目标路线图（ICLR / ICML / NeurIPS Full Paper）

> 目标档位：**主会完整论文**，不是 workshop / D&B 短文。  
> 验收标准见 §3；未达标前不自称 SOTA。

---

## 1. 论文主张（2026-09-01 晚：**第三版的两条效用轴未通过可区分性检验，第四版**）

### 1.0 现状（2026-09-01 晚更新）

主张经历三次否定，这是第四版：

1. v1「CAF 比保真指标更能预测规划效用」——被否（全池 Δ=+0.043，CI 跨 0）。
2. v2「保真度 vs 可控性在动作量级上存在交叉点」——被否。交叉点只在动作扰动
   构造的模型园里出现；换成 18 个独立训练模型后消失。
3. v3「排序由效用轴选择 + 模型园构造共同决定」——**被否**。v3 的 (a) 依赖
   「`mppi_cos` / `cl_progress` 上可控性胜、`gt_rank` 上保真度胜」这一分歧，
   但可靠性与 oracle 上界检验表明：**那两条轴根本无法区分模型**，
   分歧是测量失效，不是发现。见 §1.1。
4. v4（下）——把这次检验本身变成贡献，并给出修好的轴。

**第四版主张**：

> **「世界模型评测里的『下游效用』代理指标，多数在真实预算下无法区分模型；
> 这一点从未被检验，而它决定了所有『指标 vs 效用相关性』结论的可信度。」**
>
> (a) **诊断协议**：任一效用轴上报相关性之前，必须先过两关——
>     *可靠性*（分半重测，系统排序能否复现）与
>     *可区分性*（近完美动力学的 oracle 得分是否显著高于模型园）。
> (b) **实证**：三条常用轴中两条不合格。`mppi_cos` 要求从终点目标反推首步动作，
>     而该量**不可辨识**（oracle +0.31，逐锚点 sd 0.70，模型园最好 +0.26）；
>     `cl_progress` 的 oracle（+0.285）**低于**模型园最好值（+0.383）。
>     两者都不是预算问题——加采样无法挽救。
> (c) **修复**：`plan_cost_corr`（PCC）——把「反推动作」换成
>     「对同一组候选计划的代价排序是否与真实代价一致」。目标可辨识，
>     每个锚点给 K 对配对观测而非 1 个向量，oracle 上界按构造为 1.0。
>
> 贡献 = (a) 可靠性 + 可区分性诊断协议（通用，成本极低）；
> (b) 真实机器人数据 + 28 模型预注册训练园上的实测审计；
> (c) 修复后的效用轴 PCC + 无仿真器反事实指标 CACA。

**目前唯一通过检验的既有轴是 `gt_rank_pct`**（跨 seed +0.99，跨层 +0.89），
因此下表中仅 `−gt_rank` 列可信；`mppi_cos` / `cl_progress` 两列**已撤回**，
保留仅为展示不合格轴会产出什么样的「结论」：

| 层 | 平均 \|a\| | Δ vs `−gt_rank`（**可信**） | ~~Δ vs `mppi_cos`~~ | ~~Δ vs `cl_progress`~~ |
|----|-----------|------------------------------|---------------------|------------------------|
| q0 | 0.0085 m | −0.310 | ~~+0.367~~ | ~~+0.440 (P=0.038)~~ |
| q1 | 0.0304 m | −0.198 | ~~+0.080~~ | ~~+0.169~~ |
| q2 | 0.0500 m | −0.097 | ~~+0.268~~ | ~~+0.070~~ |
| q3 | 0.0749 m | −0.072 | ~~+0.386 (P=0.029)~~ | ~~+0.138~~ |
| q4 | 0.1180 m | −0.161 | ~~+0.336 (P=0.032)~~ | ~~+0.407 (P=0.017)~~ |

读法（修订）：在唯一可信的轴上，**保真度指标每一层都胜**
（`neg_pred_id_ratio_h1` ρ=0.75–0.95），可控性指标未能取胜。
这对本项目原本想推的 CACA 是负面结果，必须如实报告；
CACA 能否在 PCC 上取胜是尚未回答的开放问题（§7.6）。

### 1.1 可靠性与可区分性审计（**新的核心结果**）

两项检验，代码 `code/metric_reliability.py`、`code/utility_oracle.py`：

**可靠性**（18 系统 × 5 层 × 3 seed，主分析）：

| 指标 | 跨 seed | 跨层 | 判定 |
|------|---------|------|------|
| `gt_rank_pct` | +0.99 | +0.89 | 合格 |
| `pred_id_ratio_h1` | +1.00 | +0.81 | 合格 |
| `fact_align` | +1.00 | +0.78 | 合格 |
| `gap_ratio` | +1.00 | +0.69 | 合格 |
| `pv_ratio` | +1.00 | +0.58 | 边缘 |
| `caca` | +1.00 | +0.46 | 偏低 |
| `cf_align` | +1.00 | +0.44 | 偏低 |
| `cl_progress` | +0.54 | +0.28 | **不合格** |
| `mppi_cos` | +0.15 | +0.01 | **不合格** |

注意：离线指标跨 seed 恒为 +1.00 是**确定性的确认，不是复现证据**——
锚点在 seed 间固定，seed 只随机化规划。因此离线指标唯一的可靠性信息在跨层列。

**可区分性（oracle 上界）**：用 `ProprioNNSim`（末端执行器空间最近邻转移模型，
不依赖任何被测模型）作为近完美动力学，在同样三条轴上打分：

| 轴 | oracle | 模型园最好 | 判定 |
|----|--------|-----------|------|
| `mppi_cos` | +0.3148 (sd 0.6982) | +0.2593 | 近完美模型与模型园无法分开 |
| `cl_progress` | +0.2850 (sd 0.6140) | +0.3828 | oracle **低于**模型园 |
| `gt_rank_pct` | +0.0055 | +0.0034 | 两端都贴地板（见下） |

三条轴彼此的一致性也证实了这点（跨 18 系统的 Spearman，逐层平均）：
`mppi_cos` vs `−gt_rank` = −0.046，`mppi_cos` vs `cl_progress` = +0.204，
`−gt_rank` vs `cl_progress` = +0.189。**本该测同一件事的三条轴互不相关。**

`gt_rank_pct` 的注意事项：它可靠且能把塌缩模型（`lam0` = 0.45）与正常模型
（0.003–0.12）分开，但正常模型之间被压缩到接近 0，**主要在区分「坏 vs 能用」
而非给质量分级**。这限制了它承载 §1.0 表中细微 Δ 的能力，需在论文中明说。

### 1.2 模型园里的对抗样本（预注册命中）

`lam0`（SIGReg 权重 0）按预注册预期**表征塌缩**：val_pred = 0.00000、
潜空间 RMS = 0.009（健康模型约 1.0）。它在评测中的表现正是可控性指标应该抓到的：
`gap_ratio` = 0.0000（动作完全不影响预测）、`caca` = −0.005、PCC = −0.005。
而比例归一化后的保真度指标 `pred_id_ratio_h1` = 0.2996 **没有**被塌缩骗过
（与 `ep_vshort` 0.329、`frac10` 0.362 同档），说明比例归一化确实起了作用——
这一点原本预期是「保真度会被塌缩骗过」，实测不成立，需要修正叙事。

### 1.3 模型园构造方式翻转结论（**结论待重测**）

> **警告**：本节的 Δ 全部基于 `mppi_cos` 与 `cl_progress`，即 §1.1 判定不合格的
> 两条轴。因此「10/10 组合朝保真度移动」这一结论目前**不可引用**，
> 必须在 `gt_rank_pct` 与 PCC 上重测（§7.6）。以下表格仅存档。

**同一批 21 个 checkpoint、同一份留出数据、同一套指标、同样 3 seed。
唯一变量：是否加入 4 条动作扰动管线（`zero/neg/swap/atten`）。**

Δ = 最佳可控性 − 最佳保真度（正 = 可控性胜）：

| 层 | 平均 \|a\| | `mppi_cos` 仅 id | `mppi_cos` +扰动 | `cl_progress` 仅 id | `cl_progress` +扰动 |
|----|-----------|------------------|------------------|---------------------|---------------------|
| q0 | 0.0085 m | **+0.418** | −0.037 | +0.073 | −0.097 |
| q1 | 0.0304 m | +0.096 | −0.025 | +0.077 | **−0.213** (P=1.000) |
| q2 | 0.0500 m | **+0.352** | −0.048 | +0.097 | **−0.170** (P=0.975) |
| q3 | 0.0749 m | +0.260 | +0.055 | −0.022 | −0.001 |
| q4 | 0.1180 m | +0.187 | +0.057 | +0.174 | **−0.107** (P=0.972) |

**10 个「分层 × 效用轴」组合全部朝保真度方向移动，无一例外。**
且两侧都有高置信度：加扰动后保真度在 `cl_progress` 上以 P=0.972–1.000 取胜；
不加扰动时可控性在主分析中以 P=0.017–0.038 取胜（§1.0）。

机制（§6.4 已诊断）：动作扰动**必然**同时破坏一步保真度，因为真实下一状态本身依赖真实动作。
于是保真度指标在动作扰动园里的「预测效用」能力，有一部分是园子构造赋予的，
而不是来自它所度量的性质。换成质量差异来自容量/预算/正则的真实模型后，这份人为优势消失。

> **给该领域的方法学结论：不要用动作扰动构造的模型园来验证世界模型指标。**
> 这个批评可以独立成为论文的一节，且适用于任何用「破坏输入」造质量谱的指标验证工作。

附注：在动作扰动园里 `caca` 依然很强（vs `−gt_rank` ρ=0.79–0.89），
只是 `pred_id_ratio` 略高。翻转来自保真度被抬高，而非 `caca` 变弱。

### 1.4 模型园敏感性（必须报告）

同一批 ckpt，园子从 15 系统扩到 63 系统后，「最佳指标」换人：
- 15 系统：`caca` 单独最好（+0.796），复合 `caf_caca` 腰斩到 +0.379。
- 63 系统（q2）：复合 `caf_caca` 最好（+0.683 > `caca` +0.635）。

**结论：不能只用一个园子宣布「最佳指标」。** 这是对该类评测方法论的有效批评，写进论文。

### 1.5 度量定义（尺度无关）

设 `f` 为 WM 一步 plan-step 预测，`a` 为事实动作，`a'` 为反事实动作，`z_next(·)` 为真实结果。

- 敏感性：`gap_ratio = MSE(f(z,a), f(z,−a)) / MSE(z, z_next(a))` —— **实测与效用无关（ρ=−0.04），不要单用**
- **CACA**（目前最好的可控性量，ρ=+0.796）：  
  `caca = cos( f(z,a') − f(z,a) , z_next(a') − z_next(a) )`  
  两侧都取差分 → **共模漂移抵消**，只剩动作可归因分量；符号翻转模型直接得负值；  
  动作盲模型 `Δpred≈0` → 按定义置 0。
- **不要做复合**：`tanh(gap_ratio)·(1+caca)/2` 实测 ρ 掉到 +0.379（几乎腰斩）。
- **PCC（新，修复后的效用轴，不是指标）**：对同一锚点采样 K 条候选计划，
  `pcc = spearman( 模型代价(候选) , 真实代价(候选) )`，真实代价由 `ProprioNNSim`
  前推给出。相对 `mppi_cos` 的三点改进：目标可辨识（每条计划只有一个真实代价）、
  每锚点 K 对配对观测、oracle 上界按构造为 1.0 因而可区分性可验证。
  模型侧 rollout 与 `gt_rank_pct` 共用，**额外开销近似为零**。
  冒烟测试（3 模型 × 24 锚点）：`lam0`（塌缩）−0.005，健康模型 +0.11 / +0.21。

为什么不用总位移对齐：真实一步位移绝大部分与动作无关，`cos(位移)` 被共模稀释
（符号翻转模型 `cf_align` 仍为 **+0.087**，几乎不受惩罚；`caca` 为负）。

---

## 2. 主会「杀手实验」清单（缺一不可）

| # | 实验 | 通过标准 | 状态 |
|---|------|----------|------|
| K1 | ≥2 个 **action-conditioned** WM 上算可控性 + 保真指标 | ~~CAF 全面更好~~ → ~~动作幅度交叉点~~ → **改**：符号由效用轴决定，且模型园构造方式可翻转结论 | ✅ **达标**：**21 个真实 ckpt**（18 自训练 + 3 已发布），留出集，3 seed。可控性在 `cl_progress` 上 Δ=+0.44/+0.41（P=0.038/0.017），保真度在 `−gt_rank` 上全层取胜（ρ 至 0.95）。§1.3 给出受控翻转对照 |
| K2 | 下游 proxy：短视界动作误差 / 开环规划成功率 / policy-eval rank fidelity | 报 Spearman + 置信区间 | ✅ **三条效用轴齐备**：`gt_rank`（并列校正）、`mppi_cos`、**`cl_progress`（闭环，模型无关 proprio NN 预言机）**，均带三种聚类 bootstrap |
| K3 | 真 backbone 上 Mem Pareto | 非退化前沿；至少一配方支配「只加长 L」 | ⚠️ 推理期干预**否定**（§6.3）；改为**训练** `mem1/2/3/5/8`（同 batch 同预算，避免 SIGReg 批统计混淆），5 个 ckpt 已就绪，待画 Pareto |
| K4 | Excitation：真数据或真模型微调对照（可缩小） | demo vs high_exc 在 CF-PV 上可复现合成结论 | ⚠️ 仅合成 |
| K5 | 消融：错误 CF 协议（全序列 negate）vs 正确协议 | 错误协议破坏单调/可解释性 | ✅ 合成已有 |

**合成部分（已完成）只算 Methods 动机与协议正确性，不算主会主结果。**

---

## 3. 投稿验收（自我打分）

主会可投门槛（建议全满足）：

- [ ] K1 + K2 完成，且主表有 **≥2 backbone × ≥2 指标族**  
- [ ] K3 至少一张可引用 Pareto 图（真模型）  
- [ ] 开源：评测代码 + 复现脚本 + 合成协议  
- [ ] Related：与 What-If / WMBench / Astra / WorldMem / 可辨识性理论边界清晰  
- [ ] 局限诚实：不宣称 fidelity SOTA；卖 predictive validity + 机制

未满足 → 最多 D&B / workshop，不投主会碰运气。

---

## 4. 执行序（严格）

```text
P0  权重：LeWM（最小）→ Matrix-Game 1.3B → Cosmos-2B
P1  评测骨架：WorldModelAdapter + CAF/Mem metrics（本周）
P2  K1 on LeWM（latent CAF，最快闭环）
P3  K2 下游 proxy（MPC/CEM on LeWM 或开环动作误差）
P4  K3 小网格（有视频 WM 后再做）
P5  写 intro/related/experiments 初稿
```

**停做：** 无增量合成空转、Wan-TI2V 冒充 CAF、HF 无策略盲下。

---

## 5. 时间盒（建议）

| 周 | 交付 |
|----|------|
| W1 | 评测骨架 + LeWM 权重与 latent CAF 最小表 |
| W2 | K2 proxy + 多 seed；开始 Matrix/Cosmos 通路 |
| W3–4 | K1 扩到第 2 个 WM；K3 若有视频 WM |
| W5–6 | 主表/图定稿 + 正文 |

截止日期按目标会议倒推；权重若 W2 仍无 → 降级选项 B（两篇）或改投 D&B，**不硬冲主会空壳**。

---

## 6. 方法学硬约束（K2 教训，写入论文）

| 轮次 | 结果 | 含义 |
|------|------|------|
| K2v1 | on-policy plan：fact_err ≻ CAF | 像素/事实误差可虚高相关 |
| K2v2 | CF plan 排序 film≻ignore≻weak，但 **CAF 反序**（weak 最高） | raw `gap/(pv+ε)` 奖励 OOD gap 膨胀 |
| K2v3 | `caf_gated` 可把 film 置顶；难稳胜 fact（3 模共线） | 门控必要但不充分 |
| K2v4 | **2/3 pass**；seed0 gated ρ=1.0 ≫ fact 0.4 | 合成动机成立；主结果仍需真 WM |
| 真模型链路 | 通道序 RGB/BGR 弄反 → `pred/id` 从 0.249 变 143；latent 尺度不可比；`gt_rank` 给退化代价面满分 | **三条必写的协议修正**（见 §6.1） |
| **真模型 K1/K2** | `caca` +0.796 vs `fact_align` +0.754，**Δ 的 CI 跨 0** | 合成动机**未能**迁移到真模型；主张必须改写（§1.0） |

### 6.1 跨模型评测的三条硬修正（真模型实测得出）

0. **通道序验收**：数据集存 OpenCV **BGR**，官方推理不转换。喂 RGB 会让编码器塌缩，
   `pred/id` 从 0.249 恶化到 **143**（v4b 到 517）。`code/diag_lewm.py` 作为**强制验收测试**：
   若 `pred/id > 20` 即判定 harness 有 bug，而非模型差。
1. **潜空间尺度归一化**：不同 ckpt 的 latent RMS 差一个量级，裸 MSE 跨模型不可比。  
   统一改为**相对基线比值**：`pred/id`（对 no-op 预测器）、`gap_ratio`（对一步自然位移）、`pv_ratio`（对 NN 状态匹配噪声底）。
2. **秩指标必须做并列校正**：action-blind 系统代价面恒定 → 朴素 `mean(c_rand < c_exp)` 得 0.0（看起来完美）。  
   改为 `better + 0.5·tied`，使退化系统落回 0.5 随机线。

### 6.3 K3 否定结果（2026-08-31）

冻结 ckpt 上的 memory-blend 干预（stale 权重 w：0→1）：

| ckpt | `instant_gap_ratio` w=0→1 | `w1/w0` | 单调？ |
|------|--------------------------|---------|--------|
| v2 | 2.14 → 2.16 | 1.01 | ✗ |
| v3 | 2.14 → 2.16 | 1.04 | ✗ |
| v4b | 8.41 → 8.66 | 1.03 | ✗ |

同时 `pred_id_ratio` 0.083 → 3.73（模型确实被破坏，但**只坏保真度**）。

**原因**：该架构动作以独立条件注入最后一个 slot，混合上下文潜变量不削弱动作通路 →
**干预与合成里的机制不同源**，故此实验无效，不能作为 Mem↔Ctrl 的证据。

**唯一正当补救**：在真实 PushT 数据（10.4GB 已在本机）上**自训练**一组记忆容量不同的 WM，得到真训练 Pareto。
未做到之前，Mem↔Ctrl 不进主张。

### 6.2 无仿真器的真实反事实 oracle（NN-CF）

真机数据没有「同一状态换动作」的配对。做法：对锚点 *i* 取潜空间最近邻 *j*，约束两者首步动作距离位于成对距离分布上分位（尺度无关）；则「在 z_i 施加 a_j」的近似真值为 z_{j,t+1}，并把 ‖z_i − z_j‖² 作为**显式噪声底**报告。`pv_ratio = pv / 噪声底`。

### 6.4 为什么真模型 K1 失败：模型园被混淆

四种动作扰动（`zero`/`neg`/`swap`/`atten`）都在破坏动作通路，而真实下一状态本身依赖真实动作，
**所以保真度当然能察觉动作被破坏**（`zero` 的 `pred/id` 从 0.249 恶化到 0.989）。

即：**在真机 PushT + frameskip 5 上，一步潜空间保真度本身就是动作敏感的，并不"对可控性盲"。**
CAF 的动机失效模式（像素指标被外观主导）在此设定不出现。
判别量是**一步信号中动作可归因的比例**——此处 `gap_ratio ≈ 2.2`，动作占主导。

## 7. 下一刀：只提统计功效，不动指标

q2 的 Δ CI 下界 = −0.011（擦边）。**唯一允许的动作是提升功效**，不许换指标凑显著。

### 7.1 聚类 bootstrap 重定了瓶颈（2026-09-01）

系统之间不独立。补做聚类重采样后（详见 `reports/2026-08-31.md` §9.0）：

- 「小动作区**保真度**更好」：按系统 / 按管线 / 按 ckpt 三种口径 **全部 P≈1.000**，稳固。
- 「大动作区**可控性**反超」：P = 0.033 / 0.053 / **0.285**——按 ckpt 聚类即失去证据。

**瓶颈不是系统数（已 63），而是只有 3 个独立模型簇。** 加动作管线无用（管线簇已 21 个），
必须加**模型侧**多样性。这否掉了原计划里的第一行（官方 sim ckpt 训在 sim PushT 上，
真机 FR3 帧对它完全 OOD，不能算同域第 4 个系统）。

### 7.2 修正后的下一刀

| 动作 | 预期收益 | 状态 |
|------|----------|------|
| **自训练模型园**（17 个预注册配置，见 §7.3） | 模型簇 3 → 20，且质量谱由容量/预算/正则产生而非破坏动作 | 进行中 |
| 预测器权重扰动变体（`--weight-noise`） | 模型侧扰动，保真度与可控性可独立退化 | 已实现 |
| 子集 400 → 1500 锚点（集群上从完整 10.4GB 重抽） | bootstrap 方差下降 | 已完成 |
| 分层 3 → 5 层 | 把「单调 + 交叉点」画成曲线 | `--strata 5`，零成本 |
| 第 3 条效用轴：闭环 receding-horizon（数据内 replay 近似） | 回应「效用轴依赖性」质疑 | 待做 |

**预注册判定（不变）**：最高分层 Δ 的 **按 ckpt 聚类** CI 下界 > 0 且最低分层 CI 上界 < 0
→ 交叉点成立，K1 按 §1.0 表述。否则如实报「趋势存在但不显著」，不投主会。

### 7.3 自训练模型园（预注册于 `code/train_zoo_spec.json`）

复现官方 recipe（`github.com/lucas-maes/le-wm`）：ViT-tiny 编码器（192/12 层/patch 14）
+ 6 层 AdaLN-zero AR 预测器，18M 参数，损失 = 潜空间一步预测 MSE + λ·SIGReg，
端到端无 EMA 无 stop-grad。**17 个配置在见到任何结果之前固定**，全部报告不筛选：

| 轴 | 配置 |
|----|------|
| seed（独立性下界） | `base_s0/s1/s2` |
| 容量 | `enc6`, `tiny`, `pred2` |
| 预算 | `ep_short`, `ep_vshort`, `frac10`, `frac30` |
| 正则 | `lam0`（有塌缩风险）, `lam1`, `lr_hi` |
| **记忆（K3）** | `ctx1/2/5/8`（+ `base_s0` 即 ctx3） |

这同时解决三件事：(a) 模型簇数；(b) 回应「你的模型园只有动作扰动」这一致命质疑；
(c) K3 拿到**训练出来的**记忆容量扫描，替代已被否掉的推理期干预。

### 7.4 有了 21 个真实 ckpt，实验设计可以升级

§6.4 诊断出的混淆是：动作扰动**必然**同时破坏一步保真度（真实下一状态本身依赖真实动作），
所以保真度在这个模型园上占优可能是设计造成的假象。
21 个独立训练的 ckpt 让我们第一次可以**完全不用动作扰动**做这个对比：

同时还有一个**数据泄漏**问题必须处理：评测子集原先从全部 500 个 episode 抽样，
而自训练模型见过其中大部分。所有 zoo 模型共用一个固定划分
（`default_rng(12345).permutation(500)[:20]`），所以评测改为**只用这 20 个留出 episode**
（1600 锚点 / 4177 帧，`data/pusht_heldout.h5`）。

反方向的不对称也要如实交代：**已发布的 3 个 ckpt 训练时见过这些 episode，我们的 18 个没见过。**
因此分三层报告：

| 分析 | 模型园 | 意义 |
|------|--------|------|
| **主分析** | 18 个自训练 ckpt × 仅 `id` 管线 = 18 系统 | 18 个独立模型簇；零动作扰动；全部在留出数据上同等评测。内部效度最高 |
| 辅分析 A | +3 个已发布 ckpt = 21 系统 | 跨越「自训练 vs 官方」，但需注明已发布 ckpt 是分布内 |
| 辅分析 B | 21 ckpt × 5 管线 = 105 系统 | 与旧设计对照，量化动作扰动把保真度的优势夸大了多少 |

三个都报。若主分析与辅分析 B 结论不一致，那本身就是论文的一个核心发现：
**用动作扰动构造模型园会系统性地高估保真度指标的预测效力。**

### 7.5 第 3 条效用轴已就位

`cl_progress`：闭环 receding-horizon，每步重规划，用**本体感觉空间的最近邻转移预言机**推进
（`ProprioNNSim`）。关键设计是状态空间用机器人记录的末端位姿，**不含任何被测模型的成分**，
所以模拟器无法偏向建它的那个潜空间；每步转移**搬运观测到的位移**而非传送到邻居的后继状态。
模型没有状态估计器，必须自己把信念往前推，所以它的漂移也算进闭环代价。

4 锚点的验证已显示正确的序：`id` = +0.119（唯一取得进展）、`neg` = −0.023、`zero` = −0.073。
注意 `zero` 的 `mppi_cos`（+0.248）反而高于 `id`（−0.175）。

> **2026-09-01 晚更正**：上面这句当时读成了「这条轴提供了新信息」。
> 全量审计后的正确读法相反——`mppi_cos` 与 `cl_progress` **都没通过可区分性检验**，
> 它们与 `gt_rank` 的分歧是测量失效。4 锚点的「正确序」是巧合：
> `cl_progress` 跨层可靠性仅 +0.28，且其 oracle 上界（+0.285）低于模型园最好值（+0.383）。
> 教训：**新效用轴上线前必须先跑 oracle 上界，不能只看几个锚点上序对不对。**

### 7.6 当前执行队列（2026-09-01 晚，按依赖排序）

1. **[跑中] 10 个记忆轴 seed 复制**（`code/train_zoo_replicates.json`）。
   原记忆扫描每个 context 长度只有一个模型，离线指标又是确定性的，
   所以「3/3 seed 一致」只确认了确定性，不是复现。K3 需要每点 3 次独立训练。
   注意：复制模型**只进 `memrep` 分析**，不进 K1/K2 主池，否则记忆轴占比
   会从 5/18 变成 15/28，等于用一个轴的重复样本改写模型园构成。
2. **重跑全量评测**，带 `--dump-units`（新增）与 PCC 轴。
   逐锚点值落盘后才能做层内分半重测，把「采样噪声」与「分层真实效应」分开——
   这是 `caca` 跨层仅 +0.46 到底是噪声还是真实效应的唯一判据。
3. **`code/reliability_curve.py`**：给出每条轴达到 r=0.8 所需的锚点数。
   预期结论：`mppi_cos` 无穷大（规格问题），PCC 远小于现预算。
4. **在 `gt_rank_pct` 与 PCC 上重测 §1.3 的动作扰动对照**。
   这是 v3 唯一可能存活的部分，也是「不要用动作扰动造模型园」这条方法学结论的依据。
5. **开放问题**：CACA 在 PCC 上能否胜过保真度指标？
   在唯一可信的既有轴 `gt_rank` 上它是**输的**（§1.0），必须如实报告。
   若在 PCC 上仍输，论文就收敛为纯诊断/协议贡献，不再主推 CACA。

## 8. 已完成的可复用资产

| 资产 | 位置 |
|------|------|
| 3 个真实 action WM bundle | `ckpts/{lewm_pusht,lewm_v3,lewm_v4b}`（本机 + CFS） |
| 完整真机数据 10.4GB | `data/pusht_lewm_fr3.h5`（本机 **+ 集群**） |
| 子集抽取（BGR 原序） | `code/extract_pusht_subset.py` |
| JPEG 压缩 + 分片 | `code/compress_subset.py` |
| harness 验收测试 | `code/diag_lewm.py` |
| K1+K2 评测（分层 + 权重扰动 + PCC + 逐锚点落盘） | `code/k1k2_real_eval.py` |
| 多 seed 汇总 + 三种聚类 bootstrap | `code/aggregate_k1k2.py` |
| K3 记忆干预（否定结果） | `code/k3_mem_ctrl_real.py` |
| **指标可靠性审计（跨 seed / 跨层）** | `code/metric_reliability.py` |
| **效用轴 oracle 上界（可区分性）** | `code/utility_oracle.py` |
| **分半重测 + Spearman-Brown 预算推算** | `code/reliability_curve.py` |
| **三分析评测调度（含预注册池隔离）** | `code/run_final_eval.py` |
| **K3 记忆 Pareto（区分评测 seed 与训练 seed）** | `code/k3_mem_pareto.py` |
| **记忆轴 seed 复制预注册** | `code/train_zoo_replicates.json` |
| **LeWM 训练器（自包含，复现官方 loss）** | `code/train_wm.py` |
| **模型园预注册 + 跨 GPU 调度** | `code/train_zoo_spec.json`, `code/train_zoo.py` |
| 训练数据 memmap 转换 | `code/prep_train_data.py` |
| 集群拉取完整数据集（16 路并行分块） | `code/cluster_fetch_dataset.sh` |
| 代码同步 | `code/sync_to_cluster.ps1` |

### 8.1 集群环境要点（踩过的坑）

- 工作目录 `/mnt/group/jxdong/wm_exp`（旧的 `/cfs` 挂载点已失效）。
- 集群**可以**访问 GitHub 与 HF LFS（单连接 ~0.45 MB/s，16 路并行聚合 ~9 MB/s）。
- `kubectl cp` 会在 ~100MB 以上掉线：数据一律在集群上生成，只同步代码（~56KB）。
- PowerShell here-string 产生 CRLF，会被远端 bash 当成参数一部分；远端命令写成单行。
- 8 卡 A800-80GB **每卡已被同节点其他租户占用 ~73GB**，仅余约 8GB 可用。
