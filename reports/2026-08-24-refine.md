# 实验完善 2026-08-24（续）

## 本轮改动

### Excitation → v4
- 加强非线性动作（更大旋转/平移）
- 更长序列 T=16、STEPS=1000
- 判定改为 **多数 seed**：high_exc 训练在 high_exc 评测上 **同时** 更低 PV 与更低 fact_err
- 附带记录 demo 的 OOD `self_cf_gap` 膨胀（不应当主指标）

### Mem–Ctrl → multiseed
- 同一 frozen-memory CF 协议
- seeds 0–4；报单调比例、`w=1→gap≈0` 比例

### 集群
- 一次 SSH 启动：GPU1=`excitation_v4`，GPU2=`mem_v3_multiseed`（25mhn）
- 少频轮询，结果写入 `runs/excitation_v4/`、`runs/mem_v3_multiseed/`

## 待收结果后更新
（本节跑完后补 summary）
