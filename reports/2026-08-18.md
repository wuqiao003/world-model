# 日报 2026-08-18（Day 1 tick）

> Worker：`slv2n`（主）；`wvk6p` 显存偏满，未用  
> 工作区：`/mnt/group/jxdong/wm_exp/`  
> 触发：`AGENT_LOOP_TICK_daily_wm`（24h 循环）

## 1. 今日进度

### 承接昨日（无新结果文件变更，直至本 tick 重启实验）
| 方案 | 状态 | 备注 |
|------|------|------|
| Idea 1 CAF | 机制成立，待挂真实 WM | 合成 film/ignore 可分 |
| Idea 2 Mem–Ctrl | **主信号保留** | Mem v3 seed1 严格单调 |
| Idea 4 Excitation | 通过（oracle PV） | 否决 self-gap 主指标 |
| Idea 3 Sel-Mem | v1 失败 → **本日开 v2** | 稀有事件 + 覆盖代价 + 稀疏/对齐损失 |
| Idea 7 几何门控 | TRELLIS 通路已通 → **本日开 verifier 玩具** | 基于 `sample.ply` |
| TRELLIS smoke | ✅ 昨日完成 | `runs/trellis_smoke/sample.ply` |

### 本日新开（tick 时启动）
- `sel_mem_v2.py`：seed 0/1，GPU 0/1 → **未通过**
  - oracle 在 `post_event_err` 上略优于 always（0.00835 vs 0.00876）
  - selective 被稀疏项压到 mean_write≈0.05，但 **事件/非事件无分辨**（p_ev≈p_ne）
  - **判决：** 稀疏正则 alone 不够；需更强 event 信号（surprise / mode-delta）进 write head，或显式 mode 监督
- `geom_verifier.py`：✅ **通过**（478k 点，extent 合理，质心近原点）

### 资源发现
- CFS 有 **Wan2.2**：`TI2V-5B` / `I2V-A14B` / `T2V-A14B` / `Animate-14B`（`/mnt/public/Wan2.2_models`）  
  → 下一步可评估 TI2V-5B 做真实 CAF（需确认 action conditioning 接口，可能只能先做无动作基线）

## 2. 否决 / 注意（累计，本日无新增否决）

- 全序列 negate 测 Mem–Ctrl ❌  
- self_cf_gap 作 Excitation 主指标 ❌  
- Sel-Mem v1 弱事件设定 ❌  
- **Sel-Mem v2**：稀疏损失让写入率≈event_rate，但无事件分辨 ❌（idea 未死，write head 特征不够）  
- 代理 clone TRELLIS / 无头 open3d / hjm trellis2 缺 kaolin ❌  

## 3. 资源快照（tick 时）

| Worker | 观察 |
|--------|------|
| **slv2n** | GPU0–3 ~2.5GB；GPU4–7 ~10GB；util≈0%。有他人 `xifgao` TRELLIS/Blender 渲染进程，**勿抢 4–7**；玩具实验用 0/1 |
| **wvk6p** | 0/1 高占用（63GB/33GB），其余 ~10GB；今日不用 |
| qdm9p / 998s5 | 继续勿碰 |

## 4. 方案优先级（不变）

1. **Mem–Ctrl Pareto（Idea 2）** — 最强合成证据  
2. **CAF（Idea 1）** — 下一步挂 Wan/Cosmos  
3. **Excitation（Idea 4）** — 配套叙事  
4. **Sel-Mem v2 / Geom verifier** — 本日迭代中  
5. Off-screen / 第三人称 — 暂缓

## 5. 下一 tick / 持续迭代

1. Sel-Mem v3：write head 输入加 prediction surprise / frame delta；去掉盲目稀疏或改成对比损失  
2. Geom verifier → 接到多 seed TRELLIS 一致性（同图多种子 ply 距离）  
3. 探 Wan2.2-TI2V-5B 推理路径（是否需 action；无则先做非条件视频对照）  
4. 继续避开他人满载卡与 xifgao 渲染占用的 GPU 4–7

## 6. 文件

- 新增脚本：`code/sel_mem_v2.py`, `code/geom_verifier.py`  
- 昨日总账：`09-cluster-experiments.md`、`reports/2026-08-17.md`
