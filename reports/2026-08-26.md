# 日报 2026-08-26（主会迭代）

> 目标：ICLR / ICML / NeurIPS **完整论文**。本日焦点：K2 predictive validity + 权重解阻。

## 进度

1. **路线图**落地：`16-main-track-roadmap.md`（K1–K5 + 投稿门槛）。
2. **评测骨架** `eval/`：`WorldModelAdapter` + `compute_caf`（含 `caf_gated` / `score_iv`）。
3. **K2 合成闭环**（集群 `25mhn`）：
   - K2v1：on-policy plan → fact_err 虚高相关，raw CAF 失败。
   - K2v2：CF plan 排序 **film ≻ ignore ≻ weak** 正确，但 **raw/cal CAF 反序**（weak 因 OOD gap 膨胀最高）→ **否决 raw CAF 作主指标**。
   - K2v3：`caf_gated` 三 seed 均把 film 排第一；Spearman≈0.5，仍不及 fact_err（本套件共线）。
   - K2v4（进行中）：`pos_only` 非对称训练 + 负 ang CF plan，制造 pixel/fact 可能误排的设定。
4. **LeWM**：本机 HF 拉取 → SCP → CFS（`69M` ckpt）。集群直连 HF/镜像超时。  
   - `eval/adapters/lewm_stub.py` 接好（ViT config/module patch for transformers 4.41）。  
   - **K1 smoke PASS**：`pass_action_sensitive=true`，`one_step_self_gap≈0.25`（合成 RGB；PV 仍为弱 stand-in）。

## 否决 / 改写

| 项 | 决定 |
|----|------|
| raw `CAF = gap/(pv+ε)` | ❌ 不单独作主会卖点（奖励 OOD gap） |
| Wan2.2-TI2V 作 CAF 主实验 | ❌ 无 action |
| 自称 SOTA | ❌ |
| 主主张 | ✅ **gated / 双轴 CAF**：敏感性 ∧ CF 有效性 ∧ fact 门控 |

## 资源

- Worker：`raytrainv2-jxdong-ray-worker-worker-25mhn`
- Runs：`k2v{2,3,4}_*`、`k1_lewm_latent_caf`
- Ckpt：`/mnt/group/jxdong/wm_exp/ckpts/lewm_pusht/`（完整 bundle）

## 下一步（严格）

1. PushT 真实帧（或 h5）上算 latent CAF + fact；接 MPPI cost 作 K2 proxy。  
2. 第 2 个 action WM（Matrix-Game / Cosmos / quentinll 官方）。  
3. 停无增量合成空转。
